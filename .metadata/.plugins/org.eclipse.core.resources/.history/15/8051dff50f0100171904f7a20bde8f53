#ifndef __ALGS_H__
#define __ALGS_H__

void FloodFillThread(chanend stream);
void JustinDenoiseRow(
    uint32_t* unsafe top,
    uint32_t* unsafe cur,
    uint32_t* unsafe bot);

void DenoiseRow(
    uint8_t* unsafe above,
    uint8_t* unsafe current,
    uint8_t* unsafe below);

#endif
