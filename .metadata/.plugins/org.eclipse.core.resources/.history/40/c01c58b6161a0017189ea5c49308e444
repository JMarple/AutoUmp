#include "algs.h"
#include <time.h>

int main()
{
    uint32_t top[10];
    uint32_t cur[10];
    uint32_t bot[10];

    srand(time(NULL));

    int i;
    for (i = 0; i < 10; i++)
    {
        top[i] = rand();
        cur[i] = rand();
        bot[i] = rand();
    }

    return 0;
}
